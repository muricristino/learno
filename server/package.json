{
  "name": "learno-server",
  "version": "1.0.0",
  "description": "Local server for the learn skill — Gemini proxy and SQLite store",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
